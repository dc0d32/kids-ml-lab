"""Kids ML Lab — the shared toolbox behind every chapter.

Notebooks and Streamlit pages both import from here, so a picture in the app and a
picture in the notebook are always produced by the exact same code.
"""

__version__ = "0.1.0"
